# Local development (SQLite, no setup required)
DATABASE_URL=sqlite+aiosqlite:///./gradjobs.db

# Production (Supabase / Railway PostgreSQL)
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# Optional: trigger an immediate scrape on startup (useful for first run)
SCRAPE_ON_STARTUP=true
